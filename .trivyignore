# Trivy ignore file for unfixable LOW-severity Debian base image CVEs
# These are inherent to python:3.11-slim (Debian Bookworm) and have no upstream fix available.
# Reviewed and accepted as low-risk for a containerized ML service.

# util-linux: partial disclosure of arbitrary files in chfn/chsh (libreadline)
# Not exploitable: chfn/chsh not used in container, non-root user
CVE-2022-0563

# shadow-utils: Default subordinate ID configuration in /etc/login.defs
# Not exploitable: no user creation at runtime
CVE-2023-4641

# systemd: sealed/encrypted settings modification and TOCTOU race
# Not exploitable: systemd not PID 1 in container
CVE-2023-7008
CVE-2023-50387
CVE-2023-50868

# tar: setuid/setgid extraction warnings and rmt side effects
# Not exploitable: tar not used at runtime, non-root user
CVE-2005-2541
CVE-2023-39804

# sysvinit: no-root option security flaw
# Not exploitable: sysvinit not used in container
CVE-2023-22652

# perl: File::Temp insecure temporary file handling
# Not exploitable: perl not used at runtime
CVE-2011-4116

# ncurses: stack buffer overflow
# Not exploitable: ncurses not used at runtime
CVE-2023-45918
CVE-2023-50495

# initscripts: insecure permissions for /var/log
# Not exploitable: initscripts not used in container
CVE-2008-1232

# login.defs / su related (CVE-2005-4890)
# Not exploitable: su not used in container, non-root user
CVE-2005-4890
CVE-2019-19882
